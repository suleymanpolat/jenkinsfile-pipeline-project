pipeline {
    agent any
    stages {
        stage('run') {
            steps {
                echo 'Clarusway_Way to Reinvent Yourself_V2'
                sh 'python --version'
                sh 'python pipeline.py'
            }
        }
    }
}